package com.example.hackhub.domain.implementazione;

import org.junit.jupiter.api.Test;

import java.lang.reflect.Method;

import static org.junit.jupiter.api.Assertions.*;

class SottomissioneTest {
/*
    @Test
    void impostaValutazione() {
        Sottomissione s = new Sottomissione("file.zip");
        Valutazione v = new Valutazione();

        s.impostaValutazione(v);

        assertEquals(v, s.getValutazione());
    }

    @Test
    void getIdSottomissione() throws Exception {
        Sottomissione s = new Sottomissione("file.zip");

        // Simula il comportamento di @PrePersist
        Method m = Sottomissione.class.getDeclaredMethod("assegnaId");
        m.setAccessible(true);
        m.invoke(s);

        assertNotNull(s.getIdSottomissione());
        assertTrue(s.getIdSottomissione().startsWith("S-"));
    }

    @Test
    void getRiferimentoFile() {
        Sottomissione s = new Sottomissione("progetto_finale.zip");

        assertEquals("progetto_finale.zip", s.getRiferimentoFile());
    }

    @Test
    void getValutazione() {
        Sottomissione s = new Sottomissione("file.zip");
        Valutazione v = new Valutazione();

        s.impostaValutazione(v);

        assertEquals(v, s.getValutazione());
    }

    @Test
    void haValutazione() {
        Sottomissione s = new Sottomissione("file.zip");

        assertFalse(s.haValutazione());

        Valutazione v = new Valutazione();
        s.impostaValutazione(v);

        assertTrue(s.haValutazione());
    }*/
}