package com.example.hackhub.testJunit.implementazione;

import org.junit.jupiter.api.Test;

import java.lang.reflect.Method;

import static org.junit.jupiter.api.Assertions.*;

class ValutazioneTest {
/*
    @Test
    void getIdValutazione() throws Exception {
        Valutazione valutazione = new Valutazione(8, "Ottimo progetto");

        Method method = Valutazione.class.getDeclaredMethod("assegnaId");
        method.setAccessible(true);
        method.invoke(valutazione);

        String id = valutazione.getIdValutazione();

        assertNotNull(id);
        assertTrue(id.startsWith("V-"));
    }

    @Test
    void getVoto() {
        Valutazione valutazione = new Valutazione(7, "Buono");

        assertEquals(7, valutazione.getVoto());
    }

    @Test
    void getDescrizione() {
        Valutazione valutazione = new Valutazione(9, "Molto interessante");

        assertEquals("Molto interessante", valutazione.getDescrizione());
    }

    @Test
    void setVoto() {
        Valutazione valutazione = new Valutazione(5, "Ok");

        valutazione.setVoto(10);

        assertEquals(10, valutazione.getVoto());
    }

    @Test
    void setDescrizione() {
        Valutazione valutazione = new Valutazione(6, "Discreto");

        valutazione.setDescrizione("Ottimo lavoro");

        assertEquals("Ottimo lavoro", valutazione.getDescrizione());
    }*/
}